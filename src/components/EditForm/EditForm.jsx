import React from 'react';

export const EditForm = ({ onChange, onSubmit }) => {
    return (
        <form className="edit-form" onSubmit={onSubmit}>

        </form>
    )
};

export default EditForm;